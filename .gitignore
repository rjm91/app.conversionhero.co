# Dependencies
node_modules/

# Next.js
.next/
out/

# Production build
build/

# Environment variables — NEVER commit these
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Vercel
.vercel

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp
.claude/

# Local data drops (CSV imports, scratch) — never commit
data/

# Client planning docs — repo is PUBLIC, so keep client material local-only.
# Commit only the convention (README), never the actual client docs.
docs/clients/*
!docs/clients/README.md
